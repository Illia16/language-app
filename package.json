{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "start": "nuxt dev --dotenv .env.test",
    "generate": "nuxt generate --dotenv .env.test",
    "generate:test": "nuxt generate --dotenv .env.test",
    "generate:prod": "nuxt generate --dotenv .env.prod",
    "preview": "nuxt preview",
    "deploy:test": "npm run generate:test && node deploy.js test",
    "deploy:prod": "npm run generate:prod && node deploy.js prod"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.1.0",
    "@nuxtjs/i18n": "^9.5.4",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@pinia-plugin-persistedstate/nuxt": "^1.2.1",
    "@pinia/nuxt": "^0.5.0",
    "nuxt": "^3.17.2",
    "sass": "^1.58.0",
    "uuid": "^11.1.0"
  }
}
